module Env exposing (..)

-- The Env.elm file is for per-environment configuration.
-- See https://dashboard.lamdera.app/docs/environment for more info.

-- Edit this file to contain the Auth0 ClientID and Host URL of your Auth0 Application.
-- Make sure to mark at least the clientId as Backend Only in your Lamdera configuration 
-- (https://dashboard.lamdera.app/docs/environment#secret-and-public-config-values)


clientId =
    ""


authHost =
    ""
